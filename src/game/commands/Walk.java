package game.commands;

import game.Game;
import world.rooms.Room;

public class Walk implements Command{

    @Override
    public void execute(Game game, String[] args) {
        String direction = args[0];
        Room[] rooms = game.getAllRooms();
        rooms[0]
        if ()
    }
}
